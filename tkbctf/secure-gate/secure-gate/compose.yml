services:
  proxy:
    build: ./proxy
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - BACKEND_HOST=backend
      - BACKEND_PORT=8080
    depends_on:
      - backend
  backend:
    build: ./backend
    environment:
      - FLAG=tkbctf{dummy}
